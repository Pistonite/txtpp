// use std::path::{Path, PathBuf};

// /// The path specified by the `temp` directive which may be relative or absolute
// #[derive(Debug, Clone, PartialEq, Eq, Hash)]
// pub struct PathSpec {
//     p: PathBuf,
// }

// impl AsRef<Path> for PathSpec {
//     fn as_ref(&self) -> &Path {
//         self.p.as_ref()
//     }
// }

// impl From<PathBuf> for PathSpec {
//     fn from(p: PathBuf) -> Self {
//         PathSpec { p }
//     }
// }
